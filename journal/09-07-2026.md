# Today I learned
Scope order Python uses (LEGB rule)

**Local**	Inside the current function

**Enclosing**	Outer function (for nested functions)

**Global**	Module level — where  engine ,  logger ,  app  live

**Built-in**	Python built-ins like  print ,  len 

-----

2- To create a database.py and put the engine there and import where needed.-> downside of this is that it will create a new connection on each call !!!

3- So we better use the fastapi approach with dep injection and yeilding the session instead . yeild takes one of the open coonections and give it back when query is done but connections are open still.

4- *** create_engine has the default connection amount of 5 ( +10 under load) and it's configurable

5- To use Annotated to both type hint and dep injection : DbSession = Annotated[Session, Depends(get_db)]

6- If I use async session instead, I get to have the luxury of fewer bottlenecks ( only connection pools instead of that + Threads)

7- When I install a package it does not get reflected inside the container because the poetry is frozen at build time and also volumes only cover the code not the packages ! so I have to rebuild the container/image.
# Today I struggled with

1- What is the best structure for a fastapi project ? where to put the database.py file ?

# Today I changed my mind about
1- Using dep injection instead of importing database.py into the main to cause circular imports and multiple database connections.

# Things I want to ask my tech lead

1- What is the best structure for a fastapi project ? where to put the database.py file ?

2- When and what should I include as a dependency in FastAPI() instance ?