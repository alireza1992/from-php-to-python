# Today I learned
1- Why do we need _**source**_ command for venv.

2- I could use pip-chill to summarize the requirements.txt file.

3- When docker build is run it reads the same directory .env file for environment variables and it can also default to something defined inside docker-compose.yml file.

4- I better put * in front of .env when I want to gitignore it (.env.prod etc...)

5- Reason why the env variables are both in app and db ( docker compose file) is that for the fast-api to know how to connect to db 

6- .TOML is a better choice for configuration files which comes after installing **Poetry** package manager !

7- Since Docker is already isolated, the nested virtualenv is pointless. so POETRY_VIRTUALENVS_CREATE=false inside the dockerfile.




# Today I struggled with

1- Configuring venv from scratch.

2- Configuring docker-compose file from scratch.



# Today I changed my mind about
Nothing.

# Things I want to ask my tech lead
1- Is it normal I always default to AI for basic stuff like configuring docker-compose.yml ?
